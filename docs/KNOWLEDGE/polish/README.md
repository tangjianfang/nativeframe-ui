# Polish Backlog

Detail work to take the library from "functional" to "professional".

Each entry file: `<YYYY-MM-DD>-<concern-slug>.md`. Each entry must have
frontmatter `severity:` (cosmetic | minor | major | blocker) and `effort:`
(trivial | small | medium | large).
