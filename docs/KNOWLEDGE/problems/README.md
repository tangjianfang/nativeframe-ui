# Problems

Pitfalls and fixes. Format: `<YYYY-MM-DD>-<slug>.md`.

Seed entry: `2026-07-20-memorydc-deleted-copy-assign.md` (the MemoryDC
copy-assign surprise that forced `OwnerDrawDC::mem_` to be `std::optional<MemoryDC>`).
